{% set name = "postgis" %}
{% set version = "2.5.0" %}
{% set sha256 = "be73abd747e9665f29748e0e7dc6b3f7b1ce98f32e3567f08259420ca10e36d5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: http://download.osgeo.org/{{ name }}/source/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    # - 0001-gdal-test.patch

build:
  number: 1000
  skip: True  # [ win ]

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cunit
    - gdal
    - geos
    - gettext
    - pcre
    - proj4
    - json-c
    - perl
    - postgresql
  run:
    - gdal
    - geos
    - gettext
    - pcre
    - proj4
    - {{ pin_compatible('json-c') }}
    - postgresql

test:
  commands:
    - shp2pgsql
    - pgsql2shp
    - raster2pgsql

about:
  home: http://postgis.net
  license: GPL2
  license_family: GPL
  license_file: LICENSE.TXT
  summary: 'PostGIS adds geometry, geography, raster and other types to the PostgreSQL database.'

  description: |
    PostGIS is a spatial database extender for PostgreSQL object-relational
    database. It adds support for geographic objects allowing location
    queries to be run in SQL.
  doc_url: http://postgis.net/docs/manual-2.3/
  dev_url: http://svn.osgeo.org/postgis/

extra:
  recipe-maintainers:
    - sodre
    - dwnoble
